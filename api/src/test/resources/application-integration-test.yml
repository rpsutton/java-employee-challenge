spring:
  application:
    name: employee-api-integration-test

server:
  port: 0

mock-api:
  base-url: http://localhost:8112/api/v1/employee
  connection-timeout: 5000
  read-timeout: 10000
  retry:
    max-attempts: 3
    initial-delay: 500
    max-delay: 2000
    multiplier: 2.0

logging:
  level:
    com.reliaquest.api: DEBUG
    org.springframework.web.reactive.function.client: DEBUG